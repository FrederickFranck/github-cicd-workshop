name: Greetings

on: push
#  schedule:
#    - cron: "*/5 * * * *"

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: hallokes 
        uses: ./.github/actions/hallokes-bokkes
        with:
          user-name: "Frédèrick"